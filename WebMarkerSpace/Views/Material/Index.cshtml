@model IEnumerable<WebMarkerSpace.Models.MaterialViewModel>
@using ApplicationCore.Domain.Enums

@{
    ViewData["Title"] = "Materiales";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="mb-0">@Localizer["Material.Index.Title"]</h1>
    @if (User.IsInRole("Administrador"))
    {
        <a asp-action="Create" class="btn btn-primary">@Localizer["Material.Index.NewMaterial"]</a>
    }
</div>

<form asp-action="Index"
      method="get"
      class="row g-2 align-items-end mb-4"
      id="form-filtro-materiales">

    <div class="col-auto">
        <label class="form-label">@Localizer["Material.Index.FilterName"]</label>
        <input type="text"
               name="nombre"
               value="@ViewBag.FiltroNombre"
               class="form-control"
               placeholder="@Localizer["Material.Index.FilterNamePlaceholder"]"
               autocomplete="off" />
    </div>

    <div class="col-auto">
        <label class="form-label">@Localizer["Material.Index.FilterCategory"]</label>
        <select name="categoria"
                class="form-control"
                asp-items="ViewBag.FiltroCategoria">
            <option value="">@Localizer["Common.AllFem"]</option>
        </select>
    </div>

    <div class="col-auto">
        <label class="form-label">@Localizer["Material.Index.FilterStatus"]</label>
        <select name="estado"
                class="form-control"
                asp-items="ViewBag.FiltroEstado">
            <option value="">@Localizer["Common.AllMasc"]</option>
        </select>
    </div>

    <div class="col-auto">
        <a asp-action="Index"
           class="btn btn-link"
           id="link-limpiar-filtro">
            @Localizer["Common.Clear"]
        </a>

        <span id="spinner-materiales"
              class="spinner-border spinner-border-sm text-secondary ms-2 d-none"
              role="status"
              aria-hidden="true">
        </span>
    </div>
</form>

<form id="af-token-form" class="d-none">
    @Html.AntiForgeryToken()
</form>

<span id="materiales-i18n" class="d-none"
      data-error-cargando="@Localizer["Material.Index.LoadError"]"></span>

<div id="materiales-resultados" aria-live="polite">
    @await Html.PartialAsync("_MaterialListPartial", Model)
</div>

@section Scripts
{
    <script src="~/js/material-index.js" asp-append-version="true"></script>
}
